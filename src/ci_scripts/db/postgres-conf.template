import os

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': os.environ['DB_USER'],
        'USER': os.environ['DB_NAME'],
        'PASSWORD': '',
        'HOST': os.environ.get('DB_HOST_IP', ''),
        'PORT': os.environ.get('DB_PORT_PORT', ''),
    }
}
